// routes/commentRoutes.js
const express = require('express');
const router = express.Router();
const Comment = require('../models/Comment');

// Define routes for commenting on posts, replying to comments, etc.

module.exports = router;
